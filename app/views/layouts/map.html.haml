- content_for :stylesheet do
  = stylesheet_link_tag 'application_map', media: "screen"

- content_for :content do
  = map(@map_options, @json)

  %header.page
    %h1=link_to 'Swing Out London', :controller => "website", :action => "index"
    .navigation.primary-nav<
      = link_to_unless_current "Socials", { action: :socials }, :class => ("current" if params[:action] == "socials") do
        %span.current Socials
      = link_to_unless_current "Classes", { action: :classes }, :class => ("current" if params[:action] == "classes") do
        %span.current Classes
      
  .listings.page
    = yield :listings
  
  = yield :scripts
  :javascript
    Gmaps.map.callback = function() {  
      //var layer = new google.maps.BicyclingLayer();
      //var layer = new google.maps.TransitLayer();
      //layer.setMap(Gmaps.map.serviceObject);
    }
  
= render template: 'layouts/application'