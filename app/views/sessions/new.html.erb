<section class="login">
  <% flash.each do |key, message| %>
    <%= content_tag :div, message, class: key %>
  <% end %>
  <h2>Admin Login</h2>
  <%= link_to "Log in with Facebook", "/auth/facebook", class: 'button facebook-button', method: :post %>
</section>
