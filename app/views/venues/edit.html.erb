<h1>Editing venue</h1>

<%= render :partial => 'form', :locals => { :action => 'Update' }%>

<div class="actions">
  <%= link_to 'Show', @venue %> |
  <%= link_to 'Back', venues_path %> |
  <%= conditional_delete_tag(@venue) %>
</div>
