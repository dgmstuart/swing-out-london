- content_for :title do
  Swing Out London's Lindy Map:
  -if @day
    on #{ @day.pluralize }

- content_for :description do
  -# This just about fits on a google search result:
  A map of all the Lindy Hop classes in London
  -if @day
    on #{ @day.pluralize },
  also classes in other swing dance styles: Charleston, Balboa, Blues, Authentic Jazz and Collegiate Shag

- content_for :listings do
  %ul
    - if flash.any?
      - flash.each do |_level, message|
        %li.banner= message
    - DAYNAMES.each do |day|
      %li
        = link_to_unless (day == @day), day.pluralize, day: day, anchor: "current" do
          %span#current= day.pluralize
  %p.note
    %em Unless otherwise stated all classes are primarily Lindy Hop
